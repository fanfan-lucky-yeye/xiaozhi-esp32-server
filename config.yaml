# 服务器基础配置(Basic server configuration)
server:
  # 服务器监听地址和端口(Server listening address and port)
  ip: 0.0.0.0
  port: 8000

prompt: |
  你是一个叫小智/小志的台湾女孩，说话机车，声音好听，习惯简短表达，爱用网络梗。
  请注意，要像一个人一样说话，请不要回复表情符号、代码、和xml标签。
  当前时间是:{date_time}，现在我正在和你进行语音聊天，我们开始吧。

# 使用完声音文件后删除文件(Delete the sound file when you are done using it)
delete_audio: true

# 具体处理时选择的模块(The module selected for specific processing)
selected_module:
  ASR: FunASR
  VAD: SileroVAD
  LLM: ChatGLMLLM
  TTS: FallbackTTS  # 使用带回退机制的TTS，当EdgeTTS失败时自动使用DoubaoTTS

ASR:
  FunASR:
    model_dir: models/SenseVoiceSmall
    output_dir: tmp/
    # 使用本地模型，不尝试下载远程模型
    local_model: true

VAD:
  SileroVAD:
    threshold: 0.5
    model_dir: models/snakers4_silero-vad
    min_silence_duration_ms: 700  # 如果说话停顿比较长，可以把这个值设置大一些

LLM:
  DeepSeekLLM:
    # 可在这里找到你的api key https://platform.deepseek.com/
    model_name: deepseek-chat
    url: https://api.deepseek.com
    api_key: 你的deepseek api key
  ChatGLMLLM:
    # 可在这里找到你的api key https://bigmodel.cn/usercenter/proj-mgmt/apikeys
    model_name: glm-4-flash
    url: https://open.bigmodel.cn/api/paas/v4/
    api_key: e69d960ea8be4c01946266eb6a0fc267.5T0PmjY85UGARO4n
  DifyLLM:
    # 建议使用本地部署的dify接口，国内部分区域访问dify公有云接口可能会受限
    base_url: 你的私有化部署的dify接口地址
    api_key: 你的dify api key

TTS:
  EdgeTTS:
    voice: zh-CN-XiaoxiaoNeural
    output_file: tmp/
    proxy: 你的代理地址  # 如果需要使用代理，请取消注释并替换为您的代理地址
  DoubaoTTS:
    # 火山引擎语音合成服务，需要先在火山引擎控制台创建应用并获取appid和access_token
    # 地址：https://console.volcengine.com/speech/service/8
    voice: BV407_V2_streaming
    output_file: tmp/
    appid: 你的火山引擎appid
    access_token: 你的火山引擎access_token
    cluster: volcano_tts
  FallbackTTS:
    # 主TTS配置，默认使用EdgeTTS
    primary:
      type: EdgeTTS
      voice: zh-CN-XiaoxiaoNeural
      output_file: tmp/
      proxy: 你的代理地址
    # 备选TTS配置，当主TTS失败时使用
    fallback:
      type: DoubaoTTS
      voice: BV407_V2_streaming
      output_file: tmp/
      appid: 你的火山引擎appid
      access_token: 你的火山引擎access_token
      cluster: volcano_tts